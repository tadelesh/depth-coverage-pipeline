steps:
- bash: |
    ls -l
    pwd
    echo "https://$(PIPELINE_USER):${PIPELINE_TOKEN}@github.com" > ~/.git-credentials
    git clone --branch pipeline https://${PIPELINE_USER}:${PIPELINE_TOKEN}@github.com/microsoft/terraform-provider-azurerm.git
    git clone --branch depth-$(TARGET)-$(ResourceProvider) https://github.com/Azure/azure-rest-api-specs.git
  displayName: 'Setup TF Test env'